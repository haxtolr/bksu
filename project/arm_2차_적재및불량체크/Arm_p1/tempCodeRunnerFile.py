
    #if msg_send == "NG" and msg_recv == 'b': # 불량일
    #    print("불량품")
    #    if send_thread is None or not send_thread.is_alive():
    #        send_thread = threading.Thread(target=ft_send)
    #        send_thread.start()